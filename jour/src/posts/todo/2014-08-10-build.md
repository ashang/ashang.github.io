

rootfs starting using debian 10.7, based on master (before 3.7.4 release)
toolchain using as default debian 10, gcc 8.3, autoconf 2.69, automake 1.16, libtool 2.4.6, binutils 2.31, etc.
build env using docker image same to rootfs
build passed with help of Tim after modified lots of xorp/pica code and DISABLED many gcc checking.
image runs OK into cli, and runs OK for normal linux using
daily builds set up for QA regression usage



