
# Hardware

intel
VMX, 
EPT,


ADM, SLAT, 
SVM
NP


# Links


https://en.wikipedia.org/wiki/Kernel-based_Virtual_Machine


https://zh.wikipedia.org/wiki/Cgroups


https://en.wikipedia.org/wiki/Libvirt




$ more selectorwindow.log.1 
VirtualBox GUI VM Selector Window 4.3.20 r96996 linux.amd64 (Nov 21 2014 14:43:48) release log
00:00:04.863453 Log opened 2015-01-27T03:22:01.180681000Z
00:00:04.863456 Build Type: release
00:00:04.863460 OS Product: Linux
00:00:04.863462 OS Release: 3.16.0-30-generic
00:00:04.863464 OS Version: #40-Ubuntu SMP Mon Jan 12 22:06:37 UTC 2015
00:00:04.863499 DMI Product Name: SVF15A18SCB
00:00:04.863514 DMI Product Version: C10J06YL
00:00:04.863944 Host RAM: 7866MB total, 6721MB available
00:00:04.863949 Executable: /usr/lib/virtualbox/VirtualBox
00:00:04.863951 Process ID: 4263
00:00:04.863952 Package type: LINUX_64BITS_UBUNTU_13_04
00:00:04.908639 UIMediumEnumerator: Medium-enumeration started...
00:00:07.541270 UIMediumEnumerator: Medium-enumeration finished!
00:00:07.715853 Failed to open "/dev/vboxdrv", errno=13, rc=VERR_VM_DRIVER_NOT_ACCESSIBLE
01:01:26.543681 UIMediumEnumerator: Machine (or snapshot) event received, ID = 4dc20a94-e7d5-454f-b94b-7c36eb58dae7
01:01:26.543713 Log rotated - Log started 2015-01-27T03:22:01.180681000Z




Solution:
	update Oracle_VM_VirtualBox_Extension_Pack-4.3.20.vbox-extpack


----

$ VBoxManage createvm --name "ubuntu1404" --register
Virtual machine 'ubuntu1404' is created and registered.
UUID: fa5a09f6-158a-435c-8575-8e9c270ceda1
Settings file: '/home/CORPUSERS/28850410/VirtualBox VMs/ubuntu1404/ubuntu1404.vbox'


$ VBoxManage modifyvm fa5a09f6-158a-435c-8575-8e9c270ceda1 --memory 2512


$ VBoxManage showvminfo fa5a09f6-158a-435c-8575-8e9c270ceda1 


$ VBoxManage storageattach fa5a09f6-158a-435c-8575-8e9c270ceda1 --storagectl myide --port 0 --device 1 --type dvddrive --medium /home/CORPUSERS/28850410/iso/ubuntu-14.04-server-amd64.iso 


$ VBoxManage startvm --type headless fa5a09f6-158a-435c-8575-8e9c270ceda1 
Waiting for VM "fa5a09f6-158a-435c-8575-8e9c270ceda1" to power on...
VM "fa5a09f6-158a-435c-8575-8e9c270ceda1" has been successfully started.


----

----

Turn off scale mode for good:

VBoxManage setextradata global GUI/Input/MachineShortcuts "ScaleMode=None"




----

Keys to 
----



Failed to load VMMR0.r0 (VERR_SUPLIB_OWNER_NOT_ROOT).

  sudo chown root:root /usr/lib




    vboxmanage internalcommands createrawvmdk -filename rawdisk.vmdk -rawdisk myimage.img -relative

    VBoxManage: error: The -relative parameter is invalid for raw images
    VBoxManage: error: The raw disk vmdk file was not created

    vboxmanage internalcommands createrawvmdk -filename rawdisk.vmdk -rawdisk myimage.img
    RAW host disk access VMDK file rawdisk.vmdk created successfully.

    file rawdisk.vmdk myimage.img 
    rawdisk.vmdk:   regular file, no read permission
    myimage.img: x86 boot sector



# mount a VirtualBox vdi image 

## convert into standard image and then mount it.

    VBoxManage clonehd --format RAW ubuntu.vdi ubuntu.img

Or

    VBoxManage convertfromraw --format VDI filename.img filename.vdi

    mount -t ext3 -o loop,rw ./ubuntu.img /mnt


## use virtualbox-fuse

    sudo apt-get install virtualbox-fuse

    sudo vdfuse -a -f /path-to-vdi-file /mnt

The entire disk will be mounted with partitions Partition1 , Partition2 naming format. Then those files can be loop mounted. For example,

    sudo mount -o loop /mnt/Parition1 /mountpoint


## Or use se qemu-nbd, the process is explained on serverfault and in this blog.

    sudo apt-get install qemu

Then you'll need to load the network block device module:

    sudo rmmod nbd
    sudo modprobe nbd max_part=16

Attach the .vdi image to one of the nbd you just created:

    sudo qemu-nbd -c /dev/nbd0 drive.vdi

    Now you will get a /dev/nbd0 block device, along with several /dev/nbd0p* partition device nodes.
  
    sudo mount /dev/nbd0p1 /mnt

    Once you are done, unmount everything and disconnect the device:

    sudo qemu-nbd -d /dev/nbd0


//

First, install the QEMU tools. In Ubuntu, you’ll find them in the qemu-kvm package. Whatever package your distribution ships which contains the qemu-nbd binary should be fine.

Load the nbd kernel module. Yes, I’m serious, the network block device module! (Note: All of the following commands require superuser permissions, so escalate your privileges in whatever way makes you most comfortable.)

    modprobe nbd

Then run qemu-nbd, which is a user space loopback block device server for QEMU-supported disk images. Basically, it knows all about weird disk image formats, and presents them to the kernel via nbd, and ultimately to the rest of the system as if they were a normal disk.

    qemu-nbd -c /dev/nbd0 <vdi-file>

That command will expose the entire image as a block device named /dev/nbd0, and the partitions within it as subdevices. For example, the first partition in the image will appear as /dev/nbd0p1.

Now you could, for instance, run cfdisk on the block device, but you will most likely want to mount an individual partition.

    mount /dev/nbd0p1 /mnt

Gadzooks! Now you can muck around inside the filesystem to your heart’s content. Go ahead and copy stuff in or out, or if you’re feeling fruity, have some chrooty: chroot /mnt.

When you’re done, unmount the filesystem and shut down the qemu-nbd service.

    umount /mnt
    qemu-nbd -d /dev/nbd0



# Floppy image

Create image

    fallocate -l 1474560 myimage.vfd

Or:

    head -c 1474560 /dev/zero > myimage.vfd


    mkfs.vfat myimage.vfd

    sudo mount -o loop myimage.vfd /mnt

Operate files there.

    sudo umount /mnt




