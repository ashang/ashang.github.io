


Hibernate和MyBatis，它們的任務就是要殺死SQL

http://www.codedata.com.tw/database/mysql-tutorial-4-expression-function/

在SQL敘述中使用字串資料的時候，字串資料的前後要使用單引號或雙引號：

用日期資料的時候，MySQL資料庫預設的日期格式是「年-月-日」。與字串資料一樣，前後也要使用單引號或雙引號：


ORDER:

SELECT
FROM
WHERE
GROUP BY
HAVING
ORDER BY
LIMIT


SELECT * FROM database.table


運算子：

優先順序    運算子  說明    範例    運算結果
1   %   餘數    7 % 3   1
1   MOD 餘數    7 MOD 3 1
1   *   乘  7 * 3   21
1   /   除  7 / 3   2.333
1   DIV 除（整數）  7 DIV 3 2
2   +   加  7 + 3   10
2   -   減  7 – 3  4

註：優先順序的數字從1開始，1表示優先權比較高，2比較低，以此類推。就跟一般數學運算的先乘除後加減一樣：在一個運算式中，優先權高的先算完，再換低優先權繼續算；同樣優先權的就由左到右計算。你也可以在運算式中使用左右括號，括號中的運算會先執行。


SELECT name, salary * 12 AS 'Annual Salary'
FROM company.employee

比較運算子：

優先順序    運算子  說明
1   =   等於
1    <=>    等於
1   !=  不等於
1   <   小於
1   <=  小於等於
1   >   大於
1   >＝ 大於等於


逻辑運算子：

優先順序    運算子  說明
1   NOT 非
2   &&  而且
2   AND 而且
3   ||  或
3   OR  或
3   XOR 互斥
「NOT」運算子比較特殊一些，在一般的需求中，比較不會用到它。

條件設定：

BETWEEN … AND …：範圍比較
IN (…)：成員比較
IS：是…
IS NOT：不是…
LIKE：像…

「<=>」在判斷一般資料的時候，跟「=」完全一樣；不過它用在判斷「NULL」資料的時候，效果跟「IS」一樣。

但是 IS NOT NULL不能用<>代替。

#LIKE

LIKE 'w%'
LIKE '%w'

LIKE 'w____'



ORDER BY salary * 12

ORDER BY 2 
2 is the second SELECT column.

甚至可以不在SELECT列里面的列排序。

LIMIT m, n 表示跳过m个显示n个


XE+Y，X * 10Y，例如5E+3，代表的數字為5000
XE-Y，X * 10-Y，例如5E-3，代表的數字為0.005


拿字串來執行算數運算的話，MySQL會先把字串中的內容轉換為數字，然後再執行算數運算


字串與字串可以執行連接的運算，就是把一些字串的內容連接起來後，產生一個新的字串。要執行字串連接的工作，可以使用「||」運算子，這個運算子在條件的判斷中是「或」的意思，如果你直接使用「||」運算子連接字串的話：

mysql_04_snap_09

 

 這是因為在預設的設定下，MySQL把「||」運算子當成數值的「或」運算，所以會出現這樣的情況；你可以透過設定MySQL的SQL模式，來改變這個預設處理方式：

 1
 SET sql_mode = 'PIPES_AS_CONCAT'
 這個設定會把「||」運算子用在字串值的時候，把它當成「連接」運算子






