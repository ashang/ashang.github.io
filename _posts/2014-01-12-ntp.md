---
title: NTP notes
date: 2010-01-02
---

```
$ sudo ntpdate 10.10.50.10
18 Jun 05:42:33 ntpdate[12119]: step time server 10.10.50.10 offset 73841336.975972 sec
```

(sudo) date -s 00:0000 （先随便设个时间）
(sudo) ntpd -s （同步）
(sudo) hwclock -w （把系统时间写进硬件）

# Conf files
```
/etc/picos/picos_start.conf

./etc/cron.daily/ntp:# The default Debian ntp.conf enables logging of various statistics to
./etc/cron.daily/ntp:statsdir=$(cat /etc/ntp.conf | grep -v '^#' | sed -n 's/statsdir [([^ ][^ ]*\)/\1/p')

./etc/default/ntpdate:# Set to "yes" to take the server list from /etc/ntp.conf, from package ntp,

./etc/dhcp/dhclient-exit-hooks.d/ntp:NTP_CONF=/etc/ntp.conf
./etc/dhcp/dhclient-exit-hooks.d/ntp:NTP_DHCP_CONF=/var/lib/ntp/ntp.conf.dhcp

./etc/init.d/ntp:if [ -e /var/lib/ntp/ntp.conf.dhcp ]; then
./etc/init.d/ntp:	NTPD_OPTS="$NTPD_OPTS -c /var/lib/ntp/ntp.conf.dhcp"
```
# CLI

```
./system/login_node.hh:    bool sync_ntp_configuration_to_file();

./system/login_node.cc:LoginNode::sync_ntp_configuration_to_file() {
./system/login_node.cc:        XLOG_ERROR("open ntp config file: %s failed", SYSTEM_NTP_CONFIG_FILE);
./system/login_node.cc:                XLOG_ERROR("sync ntp config file: write failed");
./system/login_node.cc:                XLOG_ERROR("sync ntp config file: write failed");
./system/login_node.cc:    bool rc = sync_ntp_configuration_to_file();
./system/login_node.cc:    bool rc = sync_ntp_configuration_to_file();
./system/login_node.cc:    bool rc = sync_ntp_configuration_to_file();
./system/login_node.cc:    bool rc = sync_ntp_configuration_to_file();
./system/login_node.cc:        XLOG_TRACE(_log_trace_enable, "get ntp configuration failed");
```

# OVS
```
./openvswitch-2.3.0/utilities/bugtool/ovs-bugtool.in:NTP_CONF = '/etc/ntp.conf'

./npb/htdocs/backend/app.py:from web_api.ntp_config import NtpConfig

./npb/htdocs/backend/web_api/ntp_config.py:    NTP_CONFIG_FILE = "/etc/ntp.conf"
./npb/htdocs/backend/web_api/ntp_config.py:    NTP_CONFIG_FILE_TMP = "/etc/ntp.conf.bk"
./npb/htdocs/backend/web_api/ntp_config.py:    def read_ntp_config_file():
./npb/htdocs/backend/web_api/ntp_config.py:    def write_ntp_config_file(config):
./npb/htdocs/backend/web_api/ntp_config.py:        current_config = self.read_ntp_config_file()
./npb/htdocs/backend/web_api/ntp_config.py:        res = self.write_ntp_config_file(current_config)
./npb/htdocs/backend/web_api/ntp_config.py:        current_config = self.read_ntp_config_file()
./npb/htdocs/backend/web_api/ntp_config.py:        res = self.write_ntp_config_file(new_config)
./npb/htdocs/backend/web_api/ntp_config.py:        current_config = self.read_ntp_config_file()

./npb/htdocs/backend/web_api/.idea/workspace.xml:        <option value="$PROJECT_DIR$/ntp_config.py" />
./npb/htdocs/backend/web_api/.idea/workspace.xml:    <entry file="file://$PROJECT_DIR$/ntp_config.py">
./npb/htdocs/backend/web_api/.idea/workspace.xml:    <entry file="file://$PROJECT_DIR$/ntp_config.py">

./openvswitch-2.6.0/utilities/bugtool/ovs-bugtool.in:NTP_CONF = '/etc/ntp.conf'
```

# Shell operation

```
sudo /etc/init.d/networking restart
```

# Exec files
- ntpd
- ntpdate

```
./usr/sbin/ntpdate-debian:	for f in /var/lib/ntp/ntp.conf.dhcp /etc/ntp.conf /etc/openntpd/ntpd.conf; do
```

# Packages
- ntp
- ntpdate

## ntp
```
./var/lib/dpkg/info/ntp.list:/usr/share/man/man5/ntp.conf.5.gz
./var/lib/dpkg/info/ntp.list:/etc/ntp.conf
./var/lib/dpkg/info/ntp.conffiles:/etc/ntp.conf
./var/lib/dpkg/info/ntp.md5sums:3ab4d5cefa0d74f5ca9d54e1d9bb1616  usr/share/man/man5/ntp.conf.5.gz
./var/lib/dpkg/status: /etc/ntp.conf 3e250ecaf470e1d3a2b68edd5de46bfd
```

# Notes

```
./usr/share/doc/ntpdate/README.Debian:ntpdate doesn't parse the /etc/ntp.conf file.  The program
./usr/share/doc/ntpdate/README.Debian:/etc/default/ntpdate or alternatively from /etc/ntp.conf.
```
# Behavior on reboot/restart

# Behavior on Upgrade


## NTPD

## SNTP

## ntpdate

The accuracy and reliability of ntpdate depends on the number of servers, the number of polls each time it is run and the interval between runs.

The maintainers have decided to deprecate it in favor of only using ntpd.

```
9 0-23 * * * /usr/sbin/ntpdate -s 208.43.238.3 >/dev/null

# ntpdate 91.189.94.4
 5 May 08:44:06 ntpdate[2347]: step time server 91.189.94.4 offset -28798.248667 sec
```

### 服务器压力特别大的情况下，ntpdate 会把时间对错。比如变成 1970-1-1
当时的做法就是，多执行几遍
现在知道 ntpdate 有 -B 参数，但是感觉不好用。万一服务器差个几秒，难道我还得执行几十遍？
/usr/sbin/ntpdate -B tiger.sina.com.cn

### 通过轮询指定的确定正确时间的 NTP 服务器， ntpdate 命令设置本地日期和时间。它从指定的每个服务器获得了一些样本，并应用标准 NTP 时钟过滤器和选择算法来选择最好的样本。

ntpdate 命令使用以下方法进行时间调整：

- 如果它确定时钟偏差超过 0.5 秒，它通过调用 settimeofday 子例程设置时钟时间。在引导时间，这是一个首选的方法。
- 如果它确定时钟偏差小于 0.5 秒，它通过调用 adjtime 子例程和偏移量来调整时钟时间。此方法倾向于用牺牲一些稳定性来保持漂移时钟更加准确。 当不是通过运行一个守护程序而是从 cron 命令有规则的运行 ntpdate 命令时，每一小时或两小时执行一次可以保证足够的走时精度，从而避免调整时钟。

注意事项：

- 使用很多服务器可以大幅度改善 ntpdate 命令的可靠性与精度。尽管能使用单一服务器，但您能通过提供至少三个或四个服务器以获得更好的性能。
- 如果一个类似 xntpd 守护程序的 NTP 服务器守护程序正在同一主机上运行，命令将拒绝 ntpdate 设置日期。
- 你必须有 root 权限才能在主机上运行这个命令。


### ntpdate 调整时间的方式就是我们所说的”跃变“：在获得一个时间之后，使用 settimeofday(2) 设置系统时间，这有几个非常明显的问题：

- 第一，这样做不安全。ntpdate 的设置依赖于 ntp 服务器的安全性，攻击者可以利用一些软件设计上的缺陷，拿下 ntp服务器并令与其同步的服务器执行某些消耗性的任务。由于ntpdate采用的方式是跳变，跟随它的服务器无法知道是否发生了异常（时间不一样的时候，唯一的办法是以服务器为准）。
- 第二，这样做不精确。一旦 ntp 服务器宕机，跟随它的服务器也就会无法同步时间。与此不同，ntpd 不仅能够校准计算机的时间，而且能够校准计算机的时钟。
- 第三，这样做不够优雅。由于是跳变，而不是使时间变快或变慢，依赖时序的程序会出错（例如，如果 ntpdate 发现你的时间快了，则可能会经历两个相同的时刻，对某些应用而言，这是致命的）。
- 因而，唯一一个可以令时间发生跳变的点，是计算机刚刚启动，但还没有启动很多服务的那个时候。其余的时候，理想的做法是使用 ntpd 来校准时钟，而不是调整计算机时钟上的时间。
- NTPD 在和时间服务器的同步过程中，会把 BIOS 计时器的振荡频率偏差——或者说 Local Clock 的自然漂移 (drift)——记录下来。这样即使网络有问题，本机仍然能维持一个相当精确的走时。


### 时间同步引起 oracle 不稳定的问题
```
把 ntpdate 放到 cron 中，5分钟自动同步一次。然而，这么做，常常会引会数据库ORA-01555，甚至db crash。
找到一种方法据说可以解决这个问题。即使用 ntpd -x 来同步时间 instead of　ntpdate。
ntpd 不只是一种时间服务器的 server 端，同时也可以做 client端，相当于ntpdate。
它做 client 时，与 ntpdate 的差别是，ntpdate已近淘汰，ntpd则可看作是它升级版。
　
下面的方法是讲如何在 linux 的环境中设置 ntpd：
1. 修改 /etc/ntp.conf
2. 加入 time server。每个数据库中心都应用会有一组时间服务器，可以找 sa 要， 比如国际站，加入：
server ntp1.alibaba.com
server ntp2.alibaba.com
server ntp3.alibaba.com
ntp.conf 有很多参数，都忽略就行。

3. 修改 /etc/sysconfig/ntpd， 加入 -x 参数，变成：
OPTIONS=”-U ntp -x -p /var/run/ntpd.pid”
这据说是为 db 同步所要求的。

4. 首先同步一次时间：
ntpdate time.nist.gov; hwclock –systohc
如果数据库还要在运行，建议不要这么做。待例行维护时再处理。

5. 然后启动 ntpd：
/etc/init.d/ntpd start

6. 最后加入到自动启动列表：
chkconfig –level 2345 ntpd on
```
