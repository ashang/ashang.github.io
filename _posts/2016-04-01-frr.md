



用
picos/build:debian-buster

这个image,看看出什么错
里面装了python3-dev python3-sphinx libyang1 1.0.184




time (./bootstrap.sh && ./configure --prefix=$HOME/frr.install/ && make -j)

上面这个应该能通过, 就 OK了

如果 dpkg-buildpackage  -us -uc -d
需要
frr-rpki-rtrlib


https://mirror.bjtu.edu.cn/debian/pool/main/f/frr/frr-rpki-rtrlib_7.4-1%2Bb1_amd64.deb

可是 装 这个会依赖frr


## Build frr on current picos switches

The following works on lab boxes 3.x/master, on both x86/arm.

### Install dependencies

Edit /etc/apt/sources.list

Make it as:

    deb http://mirrors.ustc.edu.cn/debian jessie main contrib non-free
    deb http://mirrors.ustc.edu.cn/debian jessie-updates main contrib non-free

    sudo apt update
    sudo apt dist-upgrade

    #To correct dependencies on libbz2-1.0
    sudo sed -i 's|1.0.6-7+deb8u2|1.0.6-7+b3|' /var/lib/dpkg/status

    #To correct dependencies on gcc-4.9-base
    sudo sed -i 's|4.9.2-10+deb8u2|4.9.2-10+deb8u1|' /var/lib/dpkg/status

    #To correct dependencies on libpython2.7-dev
    sudo sed -i 's|2.7.9-2+deb8u5|2.7.9-2+deb8u1|' /var/lib/dpkg/status

    #To correct dependencies on libexpat1-dev
    sudo sed -i 's|2.1.0-6+deb8u6|2.1.0-6+deb8u4|' /var/lib/dpkg/status

    sudo apt install libjson-c-dev git cmake autoconf libtool python-dev flex bison libpcre3-dev libreadline6-dev libreadline6 pkg-config libcap-dev

    E: Unmet dependencies. Try 'apt-get -f install' with no packages (or specify a solution).

    sudo apt install -f

    sudo ntpdate pool.ntp.org


### Notes
- Without time sync using `ntpdate`, `configure` may fail to work.
- libreadline is used for CLI.
- Without `pkg-config`, `libyang` can not be found after `make install`.
- Without `libcap-dev`, performance will be not acceptable.

### Install libyang

    cd
    git clone http://10.10.50.50/pub/libyang
    cd libyang/
    mkdir build && cd build
    cmake .. && make

    EDIT CMakeCache.txt to have line: ENABLE_LYD_PRIV:BOOL=ON

    cmake .. && make
    sudo make install

#### Note

If you run into errors here, you can do `make clean` and try again, or remove the `build` dir and try again.

    cd .. && mkdir build && cd build && cmake .. && make

### Install FRR

    cd
    git clone http://10.10.50.50/pub/frr
    cd ~/frr
    ./bootstrap.sh
    ./configure --prefix=/usr
    make -j4
    sudo make install

### Prepare for FRR

    sudo adduser frr
    sudo mkdir -p /var/tmp/frr
    sudo chmod 777 /run

### Run FRR

    ## User needs to be in group `frr` to start daemons, or you can use `sudo`
    ## Or you can run with debug output, and start vtysh in other connections
    sudo zebra && sudo vtysh

    libjson0-dev libjson-c2 libpcre3


## ons."
Share
Tweet
Description:
   When you run the command in the linux shell vtysh to connect to the quagga daemons (such as bgpd, ospfd, etc) returns  the following error "Exiting: failed to connect to any daemons"

   Just like this:

   alejandro @ miserver: ~ $ vtysh -d bgpd
   Exiting: failed to connect to any daemons.

   alejandro @ miserver: ~ $ vtysh
   Exiting: failed to connect to any daemons.


   Solution:
      The solution is to add the user that is executing vtysh to the quagga group. To do this edit the /etc/group file.
         After editing /etc/group should be something like:

         quagga:x:1003:alejandro

         You can specify multiple users doing:

         quagga:x:1003:alejandro, john


            This is necessary because vtysh tries to connect to the daemons using UNIX domain sockets and not all users (for security reasons) have access to these sockets.

            Another solution:
               Another solution might be during the compilation phase where you can specify the linux/unix group for sockets mentioned above. Example:

               ./configure --enable-vty-group = group


chmod 755 /run



 it is needed to run zebra first.

 But zebra by default occupies the stdin, so you can run vtysh from another shell.

 Also, you can put zebra to background:

     # zebra &
     # vtysh



## FPM, export set of table

# Zebra to kernel

Two means
- netlink, 4k entry
- protobuf


# RIB

# Quagga
The Quagga architecture consists of a core daemon, zebra, which acts as an abstraction layer to the underlying Unix kernel and presents the Zserv API over a Unix or TCP stream to Quagga clients. It is these Zserv clients which typically implement a routing protocol and communicate routing updates to the zebra daemon. Existing Zserv implementations are:

Quagga daemons are each configurable via a network accessible CLI (called a 'vty'). The CLI follows a style similar to that of other routing software. There is an additional tool included with Quagga called 'vtysh', which acts as a single cohesive front-end to all the daemons, allowing one to administer nearly all aspects of the various Quagga daemons in one place.

# FRR development tasks:
	1. Design doc
	2. Integrate FRR building into the PICA building system
	Makefile, Crosstoll chain...
	3. Remove RIB under SIF and protocols under Xorp
	4. FRR CLI adaption agent(Begin from BGP)
	5. SIF CLI interface parameters  with (Zerba)
	SIF sends the interfaces with Zebra.
	Investigate whether these interfaces need kept in SIF or only kept in Zebra.
	6. sif rib interface with FPM





	## TOTO

	-- Found PCRE: /usr/lib/arm-linux-gnueabi/libpcre.so
	-- Could NOT find PkgConfig (missing:  PKG_CONFIG_EXECUTABLE)
	-- Could NOT find Doxygen (missing:  DOXYGEN_EXECUTABLE)
	-- Could NOT find CMocka (missing:  CMOCKA_LIBRARIES CMOCKA_INCLUDE_DIR) (Required is at least version "1.0.0")
	-- Missing tools (devscripts, debhelper package) for building DEB package.
	-- Missing tools (rpm package) for building RPM package.
	-- Disabling tests because of missing CMocka


   74  sudo apt install build-essential
   75  sudo apt install build-essential -f
   76  sudo apt install -f
   77  sudo apt install build-essential
   78  dpkg -s cmake
   79  sudo apt install gcc
   80  dpkg -s hvv
   81  dpkg -s gcc
   82  cmake .. && make -j4
   83  sudo apt install flex bison
   84  cmake .. && make -j4
   85  sudo apt install libpcre3-dev
   86  cmake .. && make -j4
   87  ls
   88  vi CMakeCache.txt
   89  cmake .. && make -j4
   90  sudo make install
   91  cd ../..
   92  history
   93  sudo apt install libpcre3-dev
   94  ./bootstrap.sh && ./configure --prefix=/usr/ && make -j
   95  sudo apt install autoconf libtools
   96  sudo apt install autoconf libtool
   97  sudo apt install autoconf libtool
   98  history

