

@reboot echo '1-1.6' > /sys/bus/usb/drivers/usb/unbind

```
#MAILTO=''

# NOTE /etc/crontab is special, contains username fields.
#
# /etc/crontab: system-wide crontab
# Unlike any other crontab you don't have to run the `crontab'
# command to install the new version when you edit this file
# and files in /etc/cron.d. These files also have username fields,
# that none of the other crontabs do.
#     # Example of job definition:
#     # .---------------- minute (0 - 59)
#     # |  .------------- hour (0 - 23)
#     # |  |  .---------- day of month (1 - 31)
#     # |  |  |  .------- month (1 - 12) OR jan,feb,mar,apr ...
#     # |  |  |  |  .---- day of week (0 - 6) (Sunday=0 or 7) OR sun,mon,tue,wed,thu,fri,sat
#     # |  |  |  |  |
#     # m h dom mon dow usercommand
#     17 * * * *  root  cd / && run-parts --report /etc/cron.hourly
#     25 6 * * *  root  test -x /usr/sbin/anacron || ( cd / && run-parts --report /etc/cron.daily )
#     47 6 * * 7  root  test -x /usr/sbin/anacron || ( cd / && run-parts --report /etc/cron.weekly )
#     52 6 1 * *  root  test -x /usr/sbin/anacron || ( cd / && run-parts --report /etc/cron.monthly )

1 4 * * * mysqldump --max-allowed-packet=32M -uroot -ppica8 bugs |xz > /var/backups/bugs.sql.$(date +\%d).xz
#0 0,10,12,14,16,18 * * * mysqldump -uroot -ppica8 bugs |xz > /var/backups/bugs.sql.$(date +\%Y\%m\%d-\%H).xz
## After two days have passed you'll be able to view bug graphs from the Reports page.
5 0 * * * cd /srv/bugzilla && ./collectstats.pl
#*/15 * * * * cd /srv/bugzilla && ./whine.pl
15 9 * * * cd /srv/bugzilla && ./whine.pl
## for bugs to languish in an untriaged state, to issue complaints about this particular problem
55 0 * * * cd /srv/bugzilla && ./whineatnews.pl
####
#0 1 * * * rsync -vaAX --delete --exclude={/etc/network,/etc/rc.local,/etc/hostname,/dev,/mnt,/media,/proc,/run,/sys,/tmp} /* 10.10.50.16:/home/bak/dev11/
####
#@hourly rsync -vaAX --delete --exclude={/etc/network,/etc/rc.local,/etc/hostname,/dev,/mnt,/media,/proc,/run,/sys,/tmp} /* 10.10.50.22:/home/bak/dev11/
# NO '/*'
#0 3 * * * rsync -vaAX --delete --exclude={/etc/network,/etc/rc.local,/etc/hostname,/dev,/mnt,/media,/proc,/run,/sys,/tmp} /* 10.10.50.26:/home/bak/dev11/
0 0 * * * rsync -vaAX --delete --exclude={/etc/network,/etc/rc.local,/etc/hostname,/dev,/mnt,/media,/proc,/run,/sys,/tmp} / 10.10.50.22:/home/bak/dev11/
0 4 * * * date -I > /srv/files/.synctrace
0 * * * * date '+\%H\%M' > /srv/files/.synctrace
0 * * * * test 100000000 -gt $(\df /home|grep ^/dev/|awk '{print $4}') && df -T -x tmpfs -x aufs -x devtmpfs -x overlay | mail -s "Disk.`hostname`.$(date +"\%Y\%m\%d-\%H\%M\%S-\%s")" aaron
0 * * * * test 100000000 -gt $(\df /home|grep ^/dev/|awk '{print $4}') && df -T -x tmpfs -x aufs -x devtmpfs -x overlay | mail -s "Disk.`hostname`.$(date +"\%Y\%m\%d-\%H\%M\%S-\%s")" aaron.shang@pica8.com
0 * * * * test 100000000 -gt $(\df /|grep ^/dev/|awk '{print $4}') && df -T -x tmpfs -x aufs -x devtmpfs -x overlay | mail -s "Disk.`hostname`.$(date +"\%Y\%m\%d-\%H\%M\%S-\%s")" aaron
0 * * * * test 100000000 -gt $(\df /|grep ^/dev/|awk '{print $4}') && df -T -x tmpfs -x aufs -x devtmpfs -x overlay | mail -s "Disk.`hostname`.$(date +"\%Y\%m\%d-\%H\%M\%S-\%s")" aaron.shang@pica8.com

```
