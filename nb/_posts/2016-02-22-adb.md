

adb install -t me.efesser.flauncher_315_apps.evozi.com.apk
Performing Streamed Install
adb: failed to install me.efesser.flauncher_315_apps.evozi.com.apk: Failure [INSTALL_FAILED_DUPLICATE_PERMISSION: App installation is banned, because of launcher categories]


# $ adb shell df
* daemon not running; starting now at tcp:5037
* daemon started successfully
error: insufficient permissions for device: user in plugdev group; are your udev rules wrong?
See [http://developer.android.com/tools/device.html] for more information

$ lsusb
Bus 004 Device 002: ID 8087:8000 Intel Corp. Integrated Rate Matching Hub
Bus 004 Device 001: ID 1d6b:0002 Linux Foundation 2.0 root hub
Bus 001 Device 002: ID 8087:8008 Intel Corp. Integrated Rate Matching Hub
Bus 001 Device 001: ID 1d6b:0002 Linux Foundation 2.0 root hub
Bus 003 Device 001: ID 1d6b:0003 Linux Foundation 3.0 root hub
Bus 002 Device 003: ID 18d1:4ee7 Google Inc. Nexus/Pixel Device (charging + debug)
Bus 002 Device 002: ID 258a:002a SINO WEALTH Gaming KB
Bus 002 Device 001: ID 1d6b:0002 Linux Foundation 2.0 root hub


/etc/udev/rules.d/
40-usb_modeswitch.rules
60-bridge-network-interface.rules

Bus 002 Device 003: ID 18d1:4ee7 Google Inc. Nexus/Pixel Device (charging + debug)
SUBSYSTEM=="usb", ATTR{idVendor}=="18d1", MODE="0666"

#Bus 002 Device 003: ID 18d1:4ee7 Google Inc. Nexus/Pixel Device (charging + debug)

#
置：希望使用 adb 的每个用户都需要位于 plugdev 组中，并且需要为系统添加涵盖设备的 udev 规则。

plugdev 组：如果您看到一条错误消息，指出您不在 plugdev 组中，您需要将自己添加到 plugdev 组中：


sudo usermod -aG plugdev $LOGNAME
请注意，组只会在您登录时更新，因此您需要退出后重新登录，此更改才能生效。当您重新登录后，可以使用 id 检查自己现在是否已在 plugdev 组中。

udev 规则：android-sdk-platform-tools-common 软件包中包含一组适用于 Android 设备并由社区维护的默认 udev 规则。请使用以下命令添加这些规则：


apt-get install android-sdk-platform-tools-common


用 WLAN 连接到设备
Android 11（及更高版本）支持使用 Android 调试桥 (adb) 从工作站以无线方式部署和调试应用。例如，您可以将可调试的应用部署到多台远程设备，而无需通过 USB 实际连接您的设备，从而避免常见的 USB 连接问题（例如驱动程序安装方面的问题）。

如需使用无线调试，您需要使用配对码将您的设备与工作站配对。您的工作站和设备必须连接到同一无线网络。如需连接到您的设备，请按以下步骤操作：

无线 adb 配对对话框
在您的工作站上，更新到最新版本的 SDK 平台工具。
启用开发者选项下的无线调试选项。
在询问要允许通过此网络上进行无线调试吗？的对话框中，点击允许。
选择使用配对码配对设备。记下设备上显示的配对码、IP 地址和端口号（参见图片）。
在工作站上，打开一个终端并导航到 android_sdk/platform-tools。
运行 adb pair ipaddr:port。使用第 3 步中的 IP 地址和端口号。
当系统提示时，输入您在第 3 步中获得的配对码。系统会显示一条消息，表明您的设备已成功配对。


Enter pairing code: 482924
Successfully paired to 192.168.1.130:37099 [guid=adb-235XY]
（仅适用于 Linux 或 Microsoft Windows）运行 adb connect ipaddr:port。使用无线调试下的 IP 地址和端口（参见下图）。

无线 adb IP 地址和端口号
使用 Connection Assistant 排查设备连接问题
Connection Assistant 提供了逐步说明，可帮助您通过 ADB 连接来设置和使用设备。

如要启动此 Assistant，请依次选择 Tools > Connection Assistant。

Connection Assistant 在 Assistant 面板的一系列页面中提供了说明、环境控件以及已连接设备列表。根据需要，使用 Assistant 面板底部的 Next 和 Previous 按钮浏览页面：

通过 USB 连接设备：Connection Assistant 首先提示您通过 USB 连接设备，并提供 Rescan USB devices 按钮，您可以使用该按钮启动对已连接设备的新一次扫描。
启用 USB 调试：然后，Connection Assistant 会向您说明如何通过设备上的开发者选项启用 USB 调试。
重新启动 ADB 服务器：最后，如果您仍未在可用设备列表中看到您的设备，可以使用 Connection Assistant 最后一页上的 Restart ADB server 按钮。重新启动 ADB 服务器还会导致 ADB 再次扫描设备。如果您仍未在可用设备列表中看到您的设备，请尝试本页下一部分中的问题排查步骤。
解决 USB 连接问题
如果 Connection Assistant 未通过 USB 检测到您的设备，您可以尝试通过以下问题排查步骤来解决问题：

检查 Android Studio 能否连接到 Android 模拟器
如要检查问题是否是由 Android Studio 与 Android 模拟器的连接问题引起的，请按以下步骤操作：

打开 AVD 管理器。
创建一个新的 AVD（如果您还没有 AVD）。
使用 AVD 运行模拟器。
执行以下某项操作：
如果 Android Studio 无法连接到模拟器，请下载最新的 SDK 平台工具，然后重试。
如果模拟器成功启动，请检查 USB 数据线。
检查 USB 数据线
如要检查问题是否是由 USB 数据线故障引起的，请按照本部分中的步骤操作。

如果您有其他 USB 数据线：

使用辅助数据线连接设备。
检查 Connection Assistant 现在能否检测到设备。
如果未检测到设备，请再次尝试使用主数据线。
如果仍未检测到设备，则假定问题是设备所致并检查设备是否已针对开发进行设置。
如果您没有其他 USB 数据线，但有其他 Android 设备：

将辅助设备连接到计算机。
如果 Connection Assistant 可以检测到辅助设备，则假定问题是主设备所致并检查设备是否已针对开发进行设置。

如果未检测到辅助设备，则问题可能是 USB 数据线所致。

检查设备是否已针对开发进行设置
如要检查问题是否是由设备上的设置引起的，请按以下步骤操作：

按照针对开发设置设备部分中的步骤操作。
如果这未解决问题，请与原始设备制造商的客户服务代表联系以获取帮助。告诉客户服务代表，无法使用 ADB 将设备连接到 Android Studio。
RSA 安全密钥
当您将搭载 Android 4.2.2（API 级别 17）或更高版本的设备连接到计算机时，系统会显示一个对话框，询问您是否接受允许通过此计算机进行调试的 RSA 密钥。这种安全机制可以保护用户设备，因为它可以确保只有在您能够解锁设备并确认对话框的情况下才能执行 USB 调试和其他 adb 命令。

评分和评价




```
$ adb kill-server

$ adb shell df
* daemon not running; starting now at tcp:5037
* daemon started successfully
error: no devices/emulators found


$ adb start-server && adb shell df
error: insufficient permissions for device: user aaron is not in the plugdev group
See [http://developer.android.com/tools/device.html] for more information


$ sudo usermod -aG plugdev $LOGNAME


```



Link: https://wiki.archlinux.org/title/Android_Debug_Bridge


		Backup and restore
		You can also backup and restore your device with adb. Moreover, no root is required to follow the process. The commands below led to backup your device to a single file which can also be successively restored.

		The command to create a backup is

		$ adb backup -apk -shared -all -f backupFileName.ab
		The command parameters list is

		adb backup [-f <file>] [-apk|-noapk] [-shared|-noshared] [-all] [-system|nosystem] [<packages...>]
		Then confirm the process on your device's display and provide a password whether a backup password has been set before.

		The command to restore a previous backup is

		$ adb restore mybackup.ab
		Note: Remember that restoring replaces your device contents with the backup.







```
$ adb push . /sdcard/
adb: error: failed to copy './VTS_01_2.VOB' to '/sdcard/./VTS_01_2.VOB': no response: Success

$ adb push . /sdcard/
adb: error: failed to get feature set: device unauthorized.
This adb server's $ADB_VENDOR_KEYS is not set
Try 'adb kill-server' if that seems wrong.
Otherwise check for a confirmation dialog on your device.

$ adb kill-server

$ adb push . /sdcard/
* daemon not running; starting now at tcp:5037
* daemon started successfully
adb: error: failed to get feature set: device offline

$ adb kill-server

$ adb push . /sdcard/
* daemon not running; starting now at tcp:5037
* daemon started successfully
adb: error: failed to get feature set: device unauthorized.
This adb server's $ADB_VENDOR_KEYS is not set
Try 'adb kill-server' if that seems wrong.
Otherwise check for a confirmation dialog on your device.

$ adb kill-server

$ adb push . /sdcard/
adb: error: failed to get feature set: protocol fault (couldn't read status): Connection reset by peer

$ adb kill-server
cannot connect to daemon at tcp:5037: Connection refused

$ adb shell df
* daemon not running; starting now at tcp:5037
* daemon started successfully
Filesystem       1K-blocks      Used Available Use% Mounted on
rootfs             2827032      6012   2821020   1% /
tmpfs              2905060       736   2904324   1% /dev
tmpfs              2905060         0   2905060   0% /mnt
/dev/block/sde39   3096336   1643344   1436608  54% /system
/dev/block/sde38    838552    243828    578340  30% /vendor
/dev/block/sda14    253920       192    248488   1% /cache
/dev/block/sda15 117868728 110192796   7659548  94% /data
/data/media      117868728 110192796   7659548  94% /storage/emulated
```

##
adb -s HC35RW905430 shell uiautomator runtest SmartWearUiAutomationTest.jar -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c c    om.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTe    st -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.    AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aging    uitest.AgingTest -c  com.aginguitest.AgingTest -c com.aginguitest.AgingTest -c com.aginguitest.AgingTest |tee ~/Log/HC35RW905430_result10.    log



total 49200
drwxr-xr-x  6 28850410 domain_users     4096 Oct 28 15:36 aur/
-rw-r--r--  1 28850410 domain_users 14194688 Feb 29  2008 boot.img
drwxr-xr-x  4 28850410 domain_users     4096 Oct 28 15:36 data/
-rw-r--r--  1 28850410 domain_users  9854465 Feb 29  2008 fotakernel.elf
-rw-r--r--  1 28850410 domain_users  9515786 Feb 29  2008 kernel.elf
-rw-r--r--  1 28850410 domain_users 16777216 Feb 29  2008 ltalabel.ext4
drwxr-xr-x  3 28850410 domain_users     4096 Oct 28 15:36 META-INF/
drwxr-xr-x  3 28850410 domain_users     4096 Oct 28 15:36 recovery/
drwxr-xr-x 16 28850410 domain_users     4096 Oct 28 15:36 system/
drwxr-xr-x  2 28850410 domain_users     4096 Oct 28 15:36 tmp/
drwxr-xr-x  2 28850410 domain_users     4096 Oct 28 15:36 tools/
drwxr-xr-x  4 28850410 domain_users     4096 Oct 28 15:36 tweaks/

$ adb shell ls -l /sdcard1/
error: device unauthorized. Please check the confirmation dialog on your device.

$ adb shell devices
error: device unauthorized. Please check the confirmation dialog on your device.

$ adb shell device
error: device unauthorized. Please check the confirmation dialog on your device.

$ adb shell device
/system/bin/sh: device: not found


$ adb shell ls -l /sdcard1/
/sdcard1/: No such file or directory
[W44.3 Wed Oct 29] 09:36:17 [28850410@cnbjlx9076] ~/share/wok
$ adb shell ls -l
drwxr-xr-x root     root              1970-01-01 10:06 acct
-rw-r--r-- root     root          102 1970-01-01 10:06 boot.txt
drwxrwx--x system   cache             1970-01-02 08:00 cache
-rwxr-x--- root     root       284600 1970-01-01 10:06 charger
dr-x------ root     root              1970-01-01 10:06 config
lrwxrwxrwx root     root              1970-01-01 10:06 d -> /sys/kernel/debug
drwxrwx--x system   system            1970-01-02 08:00 data
-rw-r--r-- root     root          187 1970-01-01 10:06 default.prop
drwxr-xr-x root     root              1970-01-01 10:06 dev
lrwxrwxrwx root     root              1970-01-01 10:06 etc -> /system/etc
lrwxrwxrwx root     root              1970-01-01 10:06 ext_card -> /storage/sdcard1
-rw-r--r-- root     root         9831 1970-01-01 10:06 file_contexts
-rw-r----- root     root         1601 1970-01-01 10:06 fstab.qcom
-rwxr-x--- root     root       187876 1970-01-01 10:06 init
-rwxr-x--- root     root          729 1970-01-01 10:06 init.device.rc
-rwxr-x--- root     root          998 1970-01-01 10:06 init.environ.rc
-rwxr-x--- root     root         6627 1970-01-01 10:06 init.mk.rc
-rwxr-x--- root     root        25500 1970-01-01 10:06 init.qcom.rc
-rwxr-x--- root     root        21831 1970-01-01 10:06 init.rc
-rwxr-x--- root     root          292 1970-01-01 10:06 init.recovery.qcom.rc
-rwxr-x--- root     root         5780 1970-01-01 10:06 init.sony.usb.rc
-rwxr-x--- root     root          301 1970-01-01 10:06 init.superuser.rc
-rwxr-x--- root     root         1795 1970-01-01 10:06 init.trace.rc
-rwxr-x--- root     root         3915 1970-01-01 10:06 init.usb.rc
-rw-r--r-- root     root        11268 1970-01-01 08:00 logo.rle
drwxr-xr-x root     root              1970-01-01 08:00 lta-label
drwxrwxr-x root     system            1970-01-01 10:06 mnt
dr-xr-xr-x root     root              1970-01-01 08:00 proc
-rw-r--r-- root     root         2161 1970-01-01 10:06 property_contexts
drwx------ root     root              2014-10-27 07:06 root
drwxr-x--- root     root              1970-01-01 10:06 sbin
lrwxrwxrwx root     root              1970-01-01 10:06 sdcard -> /storage/emulated/legacy
-rw-r--r-- root     root          660 1970-01-01 10:06 seapp_contexts
-rw-r--r-- root     root        93412 1970-01-01 10:06 sepolicy
drwxr-x--x root     sdcard_r          1970-01-01 10:06 storage
dr-xr-xr-x root     root              1970-01-01 10:06 sys
drwxr-xr-x root     root              1970-05-27 10:33 system
drwxr-xr-x root     root              1970-01-01 10:06 tmp
-rw-r--r-- root     root        10993 1970-01-01 10:06 ueventd.qcom.rc
-rw-r--r-- root     root         7275 1970-01-01 10:06 ueventd.rc
lrwxrwxrwx root     root              1970-01-01 10:06 usbdisk -> /storage/usbdisk
lrwxrwxrwx root     root              1970-01-01 10:06 vendor -> /system/vendor
[W44.3 Wed Oct 29] 09:36:19 [28850410@cnbjlx9076] ~/share/wok
$ adb shell ls -l /sdcard1/
/sdcard1/: No such file or directory
[W44.3 Wed Oct 29] 09:36:21 [28850410@cnbjlx9076] ~/share/wok
$ adb shell ls -l /sdcard/
Display all 112 possibilities? (y or n)
[W44.3 Wed Oct 29] 09:36:21 [28850410@cnbjlx9076] ~/share/wok
$ adb devices
List of devices attached
CB5A1WLLDC  device



----


$ netstat -anlp | grep 5037
(Not all processes could be identified, non-owned process info
 will not be shown, you would have to be root to see it all.)
(standard input):5:tcp        0      0 127.0.0.1:5037          0.0.0.0:*               LISTEN      26671/adb

----
4d000b0b39065093    S4
01d12c7e7bfa93e4	n4
HC35RW905430 		HTC
05d450d700475d40	N5
9768b413	note3

SmartWearAutoTest

----------------------------------------

$ adb shell am start -a android.intent.action.VIEW -d http://google.com
Starting: Intent { act=android.intent.action.VIEW dat=http://google.com }

$ adb shell am start -a android.intent.action.MAIN -n com.android.settings/.Settings


$am start -a android.intent.action.MAIN -n com.myapp.package/com.myapp.package.myActivity

adb shell am force-stop  cn.com.conversant.swiftsync.android

adb shell am start -a android.intent.action.VIEW geo:0,0?q=beijing


am start -a android.intent.action.CALL -d tel:10086

# am start -n com.android.camera/com.android.camera.Camera


程序的入口类可以从每个应用的 AndroidManifest.xml 的文件中得到，以计算器（calculator）为例，它的

package="com.android.calculator2" …>…

由此计算器（calculator）的启动方法为：

# am start -n com.android.calculator2/com.android.calculator2.Calculator

对于 HelloActivity 这个示例工程，AndroidManifest.xml 如下所示：

package="com.example.android.helloactivity" …>

由此它的启动方法为：

# am start -n com.example.android.helloactivity/com.example.android.helloactivity.HelloActivity

其他的一些应用启动命令，如下所示：

calendar（日历）的启动方法为：

# am start -n com.android.calendar/com.android.calendar.LaunchActivity

AlarmClock（闹钟）的启动方法为：

# am start -n com.android.alarmclock/com.android.alarmclock.AlarmClock

Music 和 Video（音乐和视频）的启动方法为：

# am start -n com.android.music/com.android.music.MusicBrowserActivity

# am start -n com.android.music/com.android.music.VideoBrowserActivity

# am start -n com.android.music/com.android.music.MediaPlaybackActivity

Camera（照相机）的启动方法为：

# am start -n com.android.camera/com.android.camera.Camera

Browser（浏览器）的启动方法为：

# am start -n com.android.browser/com.android.browser.BrowserActivity

　　一般情况希望，一个 Android 应用对应一个工程。值得注意的是，有一些工程具有多个活动（activity），而有一些应用使用一个工程。例如：在 Android 界面中，Music和Video是两个应用，但是它们使用的都是packages/apps/Music这一个工程。而在这个工程的AndroidManifest.xml文件中，有包含了不同的活动（activity）。


Music 和 Video（音乐和视频）的启动方法为：

# am start -n com.android.music/com.android.music.MusicBrowserActivity

# am start -n com.android.music/com.android.music.VideoBrowserActivity

# am start -n com.android.music/com.android.music.MediaPlaybackActivity




1
2
3



Intent mIntent  = getPackageManager()
       .getLaunchIntentForPackage("com.laomo.demo");
startActivity(mIntent);




1
2
3
4
5
6
7
8
9



// 拨打电话
adb shell am start -a android.intent.action.CALL -d tel:10086
// 启动浏览器打开网页
adb shell am -a android.intent.action.VIEW -d http://laomos.tk
// 发送广播
adb shell am broadcast -a tk.laomo.START

// 启动已经安装的应用
adb shell am start tk.laomo.demo/tk.laomo.demo.MainActivity






  3 $ adb shell am start -a android.intent.action.VIEW -d http://google.com
  4 Starting: Intent { act=android.intent.action.VIEW dat=http://google.com }
  5
  6 $ adb shell am start -a android.intent.action.MAIN -n com.android.settings/.Settings
  7
  8
  9 $ adb shell am start -a android.intent.action.MAIN -n com.myapp.package/com.myapp.package.myActivity
 10
 11
 12
 13 <manifest xmlns:android="http://schemas.android.com/apk/res/android"
 14  package="com.iftitah.android.contact">
 15
 16
 17
 18 adb shell am broadcast -a android.intent.action.BOOT_COMPLETED -c android.intent.category.HOME -n net.fstab.checkit_android/.StartupReceiv    er
 19
 20
 21 Practically it turns out that you just have to add a slash after the package name.
 22
 23
 24 Settings - am start -a android.intent.action.MAIN -n
 25 com.android.settings/.Settings
 26 Browser - am start -a android.intent.action.MAIN -n
 27 com.android.browser/.BrowserActivity
 28
 29
 30
 31
 32     // starts the facebook app showing the inbox screen by using the Uri for that
 33     adb shell am start -a android.intent.action.VIEW -d facebook://facebook.com/inbox
 34     // open a vcard file from sdcard (will open contacts app :) )
 35     adb shell am start -a android.intent.action.VIEW -d file:///sdcard/me.vcard -t text/x-vcard
 36     // open an application to get content (in this case to get a jpeg picture)
 37     adb shell am start -a android.intent.action.GET_CONTENT -t image/jpeg
 38
 39
 40     adb shell dumpsys activity
 41 This will dump out a lot of information like: Sticky broadcasts, Activity stack, Running processes.



----------------------------------------
$ adb sideload gapps-jb-20130812-signed.zip
sending: 'sideload'  100%


----------------------------------------
$ adb install Desktop/weixin/weixin45android.apk
4434 KB/s (19512787 bytes in 4.297s)
	pkg: /data/local/tmp/weixin45android.apk
Failure [INSTALL_FAILED_UID_CHANGED]

Solution: remove (or rename, or backup first) related dir in /data/data, try again.


adb fork-server server

----------------------------------------
adb reboot bootloader

This will reboot your device into bootloader mode accepting fastboot commands.

fastboot oem unlock
----------------------------------------

## adb error: insufficient permissions for device

    adb shell df
    error: insufficient permissions for device

Or:

    adb devices
    List of devices attached
    ????????????    no permissions

You need to start the adb server as root.

    adb kill-server
    sudo adb start-server

To fix forever, try:

    sudo chmod a+s /opt/semc-device-environment/adb/adb

Restart the device server:

    adb kill-server
    adb start-server

## /mnt/sdcard: Transport endpoint is not connected

向 android 模拟器（emulator）的 sd 卡拷贝文件，不同平台有不同的方法：

1、linux 平台：

在 linux 下面我们可以将 sdcard mount 成一个loop设备。先创建一个目录，比如叫android_sdcard，然后执行：

mount -o loop sdcard.img android_sdcard


## Failure [INSTALL_PARSE_FAILED_NO_CERTIFICATES]
```
$ adb install GenieWidget.apk
2420 KB/s (1169681 bytes in 0.471s)
    pkg: /data/local/tmp/GenieWidget.apk
Failure [INSTALL_PARSE_FAILED_NO_CERTIFICATES]
```

## Failure [INSTALL_FAILED_CONFLICTING_PROVIDER]
```
$ adb install Mms.apk
2447 KB/s (735764 bytes in 0.293s)
    pkg: /data/local/tmp/Mms.apk
Failure [INSTALL_FAILED_CONFLICTING_PROVIDER]

```

http://davanum.wordpress.com/2007/12/11/android-how-to-poke-around-the-sqlite3-databases/



http://www.google.com.hk/url?sa=t&rct=j&q=9615++9215++modem&source=web&cd=9&ved=0CHEQFjAI&url=http%3A%2F%2Frichard-rrb.blogspot.com%2F2012%2F05%2Fsamsung-galaxy-s3-lte.html&ei=dzwWUN2sIZGyiQews4H4DA&usg=AFQjCNFsl1HBELFEvRDeH9emt9FhqAkGpQ



http://www.google.com.hk/url?sa=t&rct=j&q=9615++9215++modem&source=web&cd=9&ved=0CHEQFjAI&url=http%3A%2F%2Frichard-rrb.blogspot.com%2F2012%2F05%2Fsamsung-galaxy-s3-lte.html&ei=dzwWUN2sIZGyiQews4H4DA&usg=AFQjCNFsl1HBELFEvRDeH9emt9FhqAkGpQ

## Failure [INSTALL_FAILED_OLDER_SDK]
$ adb install Launcher2.apk
```
3833 KB/s (12030728 bytes in 3.064s)
    pkg: /data/local/tmp/Launcher2.apk
Failure [INSTALL_FAILED_OLDER_SDK]
```

## Failure [INSTALL_FAILED_SHARED_USER_INCOMPATIBLE]
```
$ adb install PinyinIME.apk
3180 KB/s (1287019 bytes in 0.395s)
    pkg: /data/local/tmp/PinyinIME.apk
Failure [INSTALL_FAILED_SHARED_USER_INCOMPATIBLE]
```


```
$ export PATH=/usr/lib/jvm/jdk1.6.0_31/bin:$PATH
$ . build/envsetup.sh
$ lunch full-eng
$ nohup make -k > make.log3 2>&1 &
$ tail -F make.log3
```

This is the intent for call

 Intent intent = new Intent(Intent.ACTION_CALL,Uri.parse("tel:"+phoneno));
   startActivity(intent);
& Dont miss the Add permission in Maniefiest
<uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>
//

    $ lsusb
    Bus 005 Device 025: ID 05ac:8205 Apple, Inc. Bluetooth HCI
    ...
    Bus 001 Device 047: ID 0bb4:0c8d High Tech Computer Corp.

    $ sudo vi /etc/udev/rules.d/51-android.rules
    SUBSYSTEM=="usb", ATTRS{idVendor}=="0bb4", MODE="0666", SYMLINK+="android%n"
    SUBSYSTEM=="usb", ATTR{idVendor}=="0451", ATTR{idProduct}=="d009", MODE="0666", SYMLINK+="omap3430"
    SUBSYSTEM=="usb", ATTR{idVendor}=="0483", ATTR{idProduct}=="8500", MODE="0666", SYMLINK+="epiusb"
    SUBSYSTEM=="usb", ATTR{idVendor}=="04CC", ATTR{idProduct}=="2B0A", MODE="0666", SYMLINK+="pnxdfu"
    SUBSYSTEM=="usb", ATTR{idVendor}=="04CC", ATTR{idProduct}=="8500", MODE="0666", SYMLINK+="epiusb"
    SUBSYSTEM=="usb", ATTR{idVendor}=="05C6", ATTR{idProduct}=="9008", MODE="0666", SYMLINK+="epiusb"
    SUBSYSTEM=="usb", ATTR{idVendor}=="0bdb", ATTR{idProduct}=="100e", MODE="0666", SYMLINK+="epiusb"
    SUBSYSTEM=="usb", ATTR{idVendor}=="0bb4", MODE="0666"
    SUBSYSTEM=="usb", ATTR{idVendor}=="18d1", MODE="0666"
    SUBSYSTEM=="usb", ATTR{idVendor}=="05c6", MODE="0666"
    SUBSYSTEM=="usb", ATTR{idVendor}=="054c", MODE="0666"



    $ sudo lsusb -v|grep idVendor
    11:  idVendor           0x8087 Intel Corp.
    89:  idVendor           0x8087 Intel Corp.
    169:  idVendor           0x03f0 Hewlett-Packard
    259:  idVendor           0x0fce Sony Ericsson Mobile Communications AB
    397:  idVendor           0x1d6b Linux Foundation
    462:  idVendor           0x1d6b Linux Foundation
    527:  idVendor           0x1d6b Linux Foundation
    594:  idVendor           0x1d6b Linux Foundation


    You can also add that into your own ~/.android/adb_usb.ini
        # ANDROID 3RD PARTY USB VENDOR ID LIST -- DO NOT EDIT.
        # USE 'android update adb' TO GENERATE.
        # 1 USB VENDOR ID PER LINE.
        0x15a2
        0x18d1


        *Manufacturer*    *USB Vendor ID*
        Acer              0502
        HTC/Google        18d1
        HTC               0bb4
        Huawei            12d1
        LG                1004
        Motorola          22b8
        Samsung           04e8
        Sony Ericsson     0fce



    It is recommended that a device node in /dev be automatically created by creating a udev rule, so that adb can find the device. This can be done by creating a file /etc/udev/rules.d/50-android.rules with containing following information:

    SUBSYSTEM=="usb", ATTRS{idVendor}=="22b8", SYMLINK+="android_adb", MODE="0666", OWNER="<username>"

    Then, change the file permission and restart udev:

    $ sudo chmod a+r /etc/udev/rules.d/50-android.rules
    $ sudo /etc/init.d/udev restart

    With this done, udev will create a link "/dev/android_adb" when the device with the vendor ID 22b8 is plugged in, and thus, adb can find the device without knowing its vendor ID.

An alternative way of letting adb find the device is to create the file ~/.android/adb.ini and add the vendor id in it like:

1
0x22b8
The first line is the count of listed vendor IDs, followed by the vendor IDs to be supported (it seems that the first line may also be omitted, leaving a vendor ID per line). With this config file, adb can also find the Android device by enumerating the usbdevfs.

Tracing ADB

This is not talking about getting Android logs or traces with "adb logcat", but tracing the execution of adb itself. The adb executable can run in two modes: a front-end client, or a background daemon communicating with devices or emulators. The front end can run as any user, but the daemon needs to run as root. This is why adb needs to be run by root the first time when the daemon needs to be started.

To trace ADB, one can set the environment variable

$ export ADB_TRACE=all
The traces from the ADB front-end will be shown on the screen, and the traces from the server will be saved in /tmp/adb.log.

References

    About udev:
        http://en.wikipedia.org/wiki/Udev

    Developing on a device:
        http://developer.android.com/tools/device.html





You need to set AP_PORT to 9999
You may need to set AP_HOST to localhost
You need to fire up the server on your device, and note which port it is
using.
You need the adb:
adb forward tcp:9999 tcp:server_port_on_device

Then it should work.

You can use telnet for a quick check to make sure your connection is up.
ie:
telnet localhost server_port_on_device

If you get a connection, the rest should be easy.

I also suggest a reread of the remote control page... it does work, and
typically does not require a lot of futzing about.


Finally DONE!!!!
actually the problem was that i was setting up the portforwarding and other stuff as a SuperUser and then running python as a normal user!


btw i have yet another question:  the server setup works only when i run Python via command line interpreter, i tried following simmilar setups for Eric IDE, IDLE but got the same error as previous... what i want is AutoComplete feature which is not available in command-line... any ideas???
    Robbie


Re: [SL4A] Unable to setup server + a few other questions...
An error message would help, but just off the top of my head, I see two possibilites:
1) It can't find the environment variable AP_PORT or AP_HOST. Either launch idle from a shell where you've already exported then, or set them internally using os.environ
2) It can't find android.py ... make sure that is in your sys.path


Yes, that works for me.
I set AP_PORT and AP_HOST in the shell, the launched "idle" from the shell.
Or you could put:
export AP_PORT=9999
export AP_HOST=localhost

into your ~/.bashrc

Then you just have to remember to adb forward when you set your server up.



//
Android will be started in recovery mode
adb reboot recovery

//
adb pull /dev/graphics/fb0 fb0
ffmpeg -vframes 1 -vcodec rawvideo -f rawvideo -pix_fmt rgb32 -s 320x480 -i fb0 -f image2 -vcodec png image.png

//

nexus one

trackball + power == fastboot, btw.
volume_down + power == hboot, which will let you boot into recovery

trackball + volume_down + power == three-finger salute, the equivalent to CTRL+ALT+DEL

//

$ adb
  -w                           - wipe the data partition
  -d                           - flash the data partition

//

$ adb devices
* daemon not running. starting it now on port 5037 *
* daemon started successfully *
List of devices attached
42583930324543435045    device

$ adb root
restarting adbd as root

## Failure [INSTALL_FAILED_ALREADY_EXISTS]
```
$ adb install myapp.apk
3069 KB/s (648875 bytes in 0.206s)
    pkg: /data/local/tmp/com_Qunar_3.apk
Failure [INSTALL_FAILED_ALREADY_EXISTS]
```

```
$ adb install myapp.apk
3108 KB/s (1007275 bytes in 0.316s)
    pkg: /data/local/tmp/ElongAndroid.apk
Success
```
Android 主要采用 Apache 作为其开源协议
如其所提供的应用程序框架及应用、Dalvik 虚拟机等均采用 Apache 协议发布
comercial friendly


Android 所继承的开源项目的代码，仍维持其原有的授权方式
所使用的 Linux 内核仍采用 GPL 发布
同样的，Qemu 虚拟机也采用 GPL
浏览器引擎 Webkit 采用 LGPL

Bionic C 库采用 BSD
授权方式影响了 Android 的架构
采用 Bionic C 库而不是 GLIBC 的原因之一是，不希望把GPL带到用户空间
后面将要介绍的 HAL 层（硬件抽象层），不在内核层中或内核层以下，而是在内核层之上，也是为了避免 GPL


Android images
boot.img
zImage
ramdisk.img
system.img
userdata.img


window system

Does not use the standard IPC of Linux, implement a binder solution.





Linux Kernel
Process Management
Schedule
Memory Menagement
File System Model
Device Driver Model
Security
Network


Android Enhancement
Alarm
Ashmem
Binder
Power Management
Low Memory Killer
Kernel Debugger
Logger
FS, YAFFS2 -> ext4



drivers/rtc/alarm.c
android.app.AlarmManager

Android Out of Memory Killer
drivers/staging/android/lowmemorykiller.c


mm/ashmem.c
Android / Anonymous Shared Memory Subsystem, android.os.MemoryFile

Binder
drivers/staging/android/binder.c
1. Driver to facilitate inter-process communication (IPC).
2. High Performance through shared memory.
3. Per-process thread pool for processing requests.
4. Reference counting, and mapping of object references across processes.
5. Synchronous calls between processes.

Driver to facilitate inter-process communication (IPC)
- High performance through shared memory
- Per-process thread pool for processing requests
- Reference counting, and mapping of object references across process
- Synchronous calls between processes.


//

Power Management
kernel/power/wakelock.c
kernel/power/userwakelock.c
kernel/power/earlysuspend.c

1. Built on top of standard Linux Power Management (PM).
2. More aggressive power management policy.
3. Components make requests to keep the power on through “wake locks”.
4. Supports different types of wake locks


Support 3 mode of Mobile Device:
LCD on, CPU active              Full Wake Lock
LCD off, CPU active             partial Wake Lock
LCD off, CPU sleep              No Wake Lock







kernel/staging/android/logger.c
Support Android Logcat Utility: Using logcat Commands, Filtering Log Output,
Controlling Log Output Format, Viewing Alternative Log Buffer,
Listing of logcat Command Options

EABI
Embedded Application Binary Interface






CONFIG_AEABI=y
CONFIG_SHMEM=y
CONFIG_ASHMEM=y
CONFIG_ARM_THUMB=y
CONFIG_ANDROID_BINDER_IPC=y
CONFIG_ANDROID_LOGGER=y
...
make zImage


#USB debug

http://developer.android.com/tools/device.html

vendor ID list

~/.android，应该有一个 adb_usb.ini 文件，如果没有就创建，然后将上面的 Vender ID “0x1782”单独一行加入到该文件，然后退出。

# ANDROID 3RD PARTY USB VENDOR ID LIST -- DO NOT EDIT.
# USE 'android update adb' TO GENERATE.
# 1 USB VENDOR ID PER LINE.
0x1782


拿到连接的手机的 USB 设备信息
. 关于本机 -->更多信息 ->概系统览->系统报告->usb->你所连接的device-->供应商ID(Vendor ID)

Or
. 打开终端，输入：system_profiler SPUSBDataType    命令 可以查看连接的 usb 设备的信息

echo "Your__Vendor_ID" > ~/.android/adb_usb.ini


#adb install Navigation.apk
$ adb install Navigation.apk
4399 KB/s (2586164 bytes in 0.574s)
        pkg: /data/local/tmp/Navigation.apk
Failure [INSTALL_FAILED_MISSING_SHARED_LIBRARY]


#pm

pm list packages [-f] [-s] [-3] [<FILTER>]
pm path <PACKAGE_NAME>
pm install [-r] <PATH_TO_APK>
pm uninstall [-k] <PACKAGE_NAME>
pm clear <PACKAGE_NAME>

pm list packages [-f] [-s] [-3] [<FILTER>]

$ adb shell pm list packages example
package:com.example.android.apis

$ adb shell pm list packages -f example
package:/data/app/com.example.android.apis-1.apk=com.example.android.apis

$ pm path com.example.android.apis
package:/data/app/com.example.android.apis-1.apk

pm install [-r] <PATH_TO_APK>

$ pm install -r /mnt/sdcard/com.example.android.apis-1.apk
        pkg: /mnt/sdcard/com.example.android.apis-1.apk
Success

pm uninstall [-k] <PACKAGE_NAME>

$ pm uninstall com.example.android.apis
Success

pm clear <PACKAGE_NAME>
$ pm clear com.example.android.apis
Success

$ pm
usage: pm list packages [-f] [-d] [-e] [-s] [-3] [-i] [-u] [FILTER]
       pm list permission-groups
       pm list permissions [-g] [-f] [-d] [-u] [GROUP]
       pm list instrumentation [-f] [TARGET-PACKAGE]
       pm list features
       pm list libraries
       pm path PACKAGE
       pm install [-l] [-r] [-t] [-i INSTALLER_PACKAGE_NAME] [-s] [-f]
                  [--algo <algorithm name> --key <key-in-hex> --iv <IV-in-hex>] PATH
       pm uninstall [-k] PACKAGE
       pm clear PACKAGE
       pm enable PACKAGE_OR_COMPONENT
       pm disable PACKAGE_OR_COMPONENT
       pm disable-user PACKAGE_OR_COMPONENT
       pm grant PACKAGE PERMISSION
       pm revoke PACKAGE PERMISSION
       pm set-install-location [0/auto] [1/internal] [2/external]
       pm get-install-location
       pm set-permission-enforced PERMISSION [true|false]

pm list packages: prints all packages, optionally only
  those whose package name contains the text in FILTER.  Options:
    -f: see their associated file.
    -d: filter to only show disbled packages.
    -e: filter to only show enabled packages.
    -s: filter to only show system packages.
    -3: filter to only show third party packages.
    -i: see the installer for the packages.
    -u: also include uninstalled packages.

pm list permission-groups: prints all known permission groups.

pm list permissions: prints all known permissions, optionally only
  those in GROUP.  Options:
    -g: organize by group.
    -f: print all information.
    -s: short summary.
    -d: only list dangerous permissions.
    -u: list only the permissions users will see.

pm list instrumentation: use to list all test packages; optionally
  supply <TARGET-PACKAGE> to list the test packages for a particular
  application.  Options:
    -f: list the .apk file for the test package.

pm list features: prints all features of the system.

pm path: print the path to the .apk of the given PACKAGE.

pm install: installs a package to the system.  Options:
    -l: install the package with FORWARD_LOCK.
    -r: reinstall an exisiting app, keeping its data.
    -t: allow test .apks to be installed.
    -i: specify the installer package name.
    -s: install package on sdcard.
    -f: install package on internal flash.

pm uninstall: removes a package from the system. Options:
    -k: keep the data and cache directories around after package removal.

pm clear: deletes all data associated with a package.

pm enable, disable, disable-user: these commands change the enabled state
  of a given package or component (written as "package/class").

pm grant, revoke: these commands either grant or revoke permissions
  to applications.  Only optional permissions the application has
  declared can be granted or revoked.

pm get-install-location: returns the current install location.
    0 [auto]: Let system decide the best location
    1 [internal]: Install on internal device storage
    2 [external]: Install on external media

pm set-install-location: changes the default install location.
  NOTE: this is only intended for debugging; using this can cause
  applications to break and other undersireable behavior.
    0 [auto]: Let system decide the best location
    1 [internal]: Install on internal device storage
    2 [external]: Install on external media

    adb shell pm list packages
    adb shell pm list packages -f

    adb shell pm block package.name
    adb shell pm unblock package.name

    adb shell pm get-install-location
    0 [auto]
    2 [external]

    adb shell pm set-install-location 2

    adb shell 'pm list packages -f' | sed -e 's/.*=//' | sort

    adb shell 'pm list packages -f' | sed -e 's/.*=//' | sed -e 's/^/adb uninstall /' | sort | less

    adb shell 'pm list packages -f' | sed -e 's/.*=//' | grep "com.foobar" | while read pkg; do adb uninstall $pkg; done;

 adb shell pm list packages %1 > packages.txt

   for /F "tokens=2 delims=:" %%a in (packages.txt) do adb shell pm uninstall %%a


 in $(pm list packages com.your.domain ) ; do
    pm uninstall $(i#*:} ;
done
If you're on the host machine and you're using something Unixy - Linux, Mac, Cygwin - then something similar will work there too, but you need to shove 'adb shell' in:

for i in $(adb shell pm list packages com.your.domain ) ; do
    adb uninstall $(i#*:} ;
done
Since you're talking about removing the packages from all connected phones, you need yet another loop:

for d in $(adb devices | sed '/List/d; /\*/d; s/device$//') ; do
    for i in $(adb -s $d shell pm list packages com.your.domain ) ; do
        adb -s $d uninstall $(i#*:} ;
    done
done

# am

* go back to home, in case back button fails to work
    adb shell am start -c android.intent.category.HOME -a android.intent.action.MAIN

# settings

=== Setting/getting settings from the command line for any user ===

 adb shell settings [--user num] get '''<namespace> <key>'''

 adb shell settings [--user num] put '''<namespace> <key> <value>'''

where '''<namespace>''' is either secure, system or global, the '''<key>''' can be found at one of the following locations:

 http://developer.android.com/reference/android/provider/Settings.Global.html
 http://developer.android.com/reference/android/provider/Settings.Secure.html
 http://developer.android.com/reference/android/provider/Settings.System.html

and the '''<value>''' should be in-line with what the setting expects.

Note: If the --user argument is not supplied, the owner user is assumed.

So for example you could run the following from the adb shell:

 adb shell settings --user 10 get secure android_id

which will return the value for:

 http://developer.android.com/reference/android/provider/Settings.Secure.html#ANDROID_ID

for the second user in a multi-user device.

Another example:

    adb shell settings put global wifi_on 0

(requires a reboot to take effect)

# Run adb over wifi


# if you have usb connected

Connect device via USB and make sure debugging is working.

    adb tcpip 5555
      -d option needed to connect to usb device when other connection persists (emulator connected to other wifi device, etc.)
    adb connect <DEVICE_IP_ADDRESS>:5555

Disconnect USB and proceed with wireless debugging.

    adb -s x.x.x.x:5555 logcat
    adb -s <DEVICE_IP_ADDRESS>:5555 usb to switch back when done.

No root required!

To find the IP address of the device:

    adb shell netcfg


# if you don't have usb

    setprop service.adb.tcp.port 5555
    stop adbd
    start adbd

And you can disable it and return ADB to listening on USB with

    setprop service.adb.tcp.port -1
    stop adbd
    start adbd

After done, restarting in USB mode

    adb usb

# adb kernel

    adb root
    adb shell cat /proc/kmsg |grep -vE "cpu|CPU|bq27541|temp="


# get previous session kernel log (from before rebooting)

    adb shell cat /proc/last_kmsg > /sdcard/last_kmsg.txt

# Clean up the kernel log from some noise (Linux one-liner):

    adb shell cat last_kmsg.txt |grep -vE "cpu_down|stop_machine_cpu_stop|cpu_up|shutdown|CPU|temperature|capacity|bq27541_get_psp|mtp_open|adb_release|mtp_release|adb_open|_bind_config|temp=|rt5640|sensor|cpuidle|sdhci|mmcblk|tegra_uart|nvavp|pn544|tegra-i2c" >last_kmsg_clean.txt

# Creating Android script files

to quickly invoke adb-over-tcp functionality from inside the Android terminal

    su
    mount -o remount,rw -t yaffs2 /dev/block/mtdblock4 /system
    echo "su -c 'setprop service.adb.tcp.port 5555 && stop adbd && start adbd'" > /system/bin/adbt
    chmod 755 /system/bin/adbt
    mount -o ro,remount -t yaffs2 /dev/block/mtdblock4 /system
    sync

# misc

    adb -k uninstall
    adb shell netcfg
    adb shell iftop
    adb shell netstat

# Dump

    adb shell dmesg > logs\dmesg_kernel.log
    adb shell dumpstate > logs\Dumpdevicestate.log
    adb shell dumpsys > logs\dumpsystemdata.log
    adb bugreport > logs\bugreport.log

RCA:

    adb pull /data/rcalog /logs/rcalog
    adb pull /proc/last_amsslog /logs/last_amsslog
    adb pull /proc/last_kmsg /logs/last_kmsg
    adb pull /data/crashsms /logs/crashsms
    adb pull /data/anr /logs/anr
    adb pull /data/tombstone /logs/tombstone

# adb return appends a carriage return
to remove that

    sed s/\\r//g)

# run package

    pkg=$($BASE/adb shell pm path com.koushikdutta.backup)

    # apparently pm path appends a carriage return

    pkg=$(echo $pkg | cut -d : -f 2 | sed s/\\r//g)

    echo $pkg

    $BASE/adb shell << EOF

    CLASSPATH=$pkg app_process /system/bin com.koushikdutta.shellproxy.ShellRunner2 $@ &

    exit
    EOF


# Input







 24
down vote


Also, if you want to send embedded spaces with the input command, use %s

adb shell input text 'this%sis%san%sexample'

will yield

this is an example

being input.

% itself does not need escaping - only the special %s pair is treated specially. This leads of course to the obvious question of how to enter the literal string %s... which you would have to do with two separate commands.






    (obs: %s means SPACE)



Tap X,Y position:

adb shell input tap 500 1450






        To find the exact X,Y position you want to Tap go to:

        Settings > Developer Options > Check the option POINTER SLOCATION

..

    Swipe X1 Y1 X2 Y2 [duration(ms)]:

    adb shell input swipe 100 500 100 1450 100

        in this exemple X1=100, Y1=500, X2=100, Y2=1450, Duration = 100ms

The complete list of commands can be found on: http://developer.android.com/reference/android/view/KeyEvent.html

to send double quotes to the device, try the following:

    adb shell input text '\"'

=======
before and after plugging your device.

    lsusb
    Bus 001 Device 060: ID 2a45:0c02

SUBSYSTEM=="usb", ATTR{idVendor}=="xxxx", ATTR{idProduct}=="xxxx", MODE="0600", OWNER="<username>"





#

		$ adb sideload MK51.1-151221-RELEASE.zip
		loading: '../Downloads/MK51.1-z3dual-151221-RELEASE.zip'* daemon not running. starting it now on port 5037 *
		* daemon started successfully *

		sending: '../Downloads/MK51.1-z3dual-151221-RELEASE.zip'  100%



		$ adb sideload z3.l55t.signed.zip
		loading: 'z3.l55t.signed.zip'* daemon not running. starting it now on port 5037 *
		* failed to start daemon *

		* daemon not running. starting it now on port 5037 *
		* failed to start daemon *
		error: cannot connect to daemon

		$ sudo adb sideload z3.l55t.signed.zip
		loading: 'z3.l55t.signed.zip'
		sending: 'z3.l55t.signed.zip'  100%




# remount of system failed: Permission denied
adb root
restarting adbd as root


adb remount
remount of system failed: Permission denied
remount failed


Solution: enter recovery.

adb root
adb remount
remount succeeded




# adb sideload

    adb sideload update.zip

adb shell ls -l /data/media/0/clockworkmod/backup/1971-03-29.21.08.10_23.4.A.1.232/
__bionic_open_tzdata: couldn't find any tzdata when looking for localtime!
__bionic_open_tzdata: couldn't find any tzdata when looking for GMT!
__bionic_open_tzdata: couldn't find any tzdata when looking for posixrules!
-rw-rw-rw-    1 root     root      20971520 Mar 29 21:08 boot.img



# error: device unauthorized.

adb shell df
* daemon not running. starting it now on port 5037 *
* daemon started successfully *
error: device unauthorized.
This adbd's $ADB_VENDOR_KEYS is not set; try 'adb kill-server' if that seems wrong.
Otherwise check for a confirmation dialog on your device.



# hide/unhide

Android 4.4 以上使用以下命令显示和隐藏（不需要 root）:

    adb shell pm hide {package_name}
    adb shell pm unhide {packag_name}

区别： hide 命令相当于 uninstall -k 卸载应用但是保留数据 disable...


adb shell pm hide com.xiaomi.gamecenter.sdk.service
adb shell pm hide com.xiaomi.gamecenter
adb shell pm hide com.android.thememanager
adb shell pm hide com.miui.bugreport
adb shell pm hide com.miui.video
adb shell pm hide com.miui.securityadd
adb shell pm hide com.miui.securitycenter
adb shell pm hide com.lbe.security.miui
adb shell pm hide com.miui.barcodescanner
adb shell pm hide com.miui.voiceassist



# 用"adb reboot recovery"将手机切换到
Recovery 模式，然后：

> adb devices
0123456789ABCDEF        unauthorized

如果在 Normal 模式，会弹一个信息框询问允许 USB 调试吗，但现在是Recovery模式，根
本没机会弹信息框，怎么破？

A: scz@nsfocus 2014-04-07 16:43

现在的 adb 都有公私钥认证机制，只允许授权主机使用 USB 调试接口。手机端的
/data/misc/adb/adb_keys 存有授权主机的公钥，如果主机公钥不在这个文件里，就不
是授权主机，Normal 模式会弹信息框给一个设置的机会，Recovery 模式干脆就拒绝访
问。解决办法是设法将主机公钥写入 /data/misc/adb/adb_keys。

如果有手机 root，这没什么障碍。如果没有 root，其实是有GUI 操作方案的，手机上弹
信息框询问允许 USB 调试时，下面有一个"一律允许使用这台计算机进行调试"，缺省未
选中，只要选中它，效果就是主机公钥被写入 /data/misc/adb/adb_keys。这就解决了
鸡生蛋、蛋生鸡的问题，显然一般试图在 Recovery 模式下使用"adb shell"就是为了取
得 root。"CWM Recovery"本身可以在无 root 的情况下线刷进入。

问题解决后应该看到：

> adb devices
0123456789ABCDEF        recovery
> adb shell
~ # id
uid=0(root) gid=0(root)

Recovery 模式下的"adb shell"天生就是"root shell"，不需要 su。

# Install

Failure [INSTALL_FAILED_UPDATE_INCOMPATIBLE: Package com.android.calendar signatures do not match the previously installed version; ignoring!]

Failure [INSTALL_FAILED_MISSING_SHARED_LIBRARY: Package couldn't be installed in /data/app/com.miui.cleanmaster-VSa0MbqlZ4VLCfIzXYZZVQ==: Package com.miui.cleanmaster requires unavailable shared library cloud-common.jar; failing!]

Failure [INSTALL_FAILED_UPDATE_INCOMPATIBLE: Package com.android.providers.downloads.ui signatures do not match the previously installed version; ignoring!]


Failure [INSTALL_FAILED_ALREADY_EXISTS: Attempt to re-install hdpfans.com without first uninstalling.]

Failure [INSTALL_FAILED_VERSION_DOWNGRADE]

Failure [INSTALL_FAILED_MISSING_SHARED_LIBRARY: Package couldn't be installed in /data/app/com.miui.virtualsim-P-owiFJgauQKyZk_0wpOBQ==: Package com.miui.virtualsim requires unavailable shared library micloud-sdk; failing!]

Failure [INSTALL_FAILED_SHARED_USER_INCOMPATIBLE: Package couldn't be installed in /data/app/com.xiaomi.drivemode-VHFg2Cu7QIrk5xmJopNKBA==: Package com.xiaomi.drivemode has no signatures that match those in shared user android.uid.system; ignoring!]

$ adb install com.youdao.dict.5030300.v5.3.3.apk
3842 KB/s (29302955 bytes in 7.448s)
    pkg: /data/local/tmp/com.youdao.dict.5030300.v5.3.3.apk
Failure [INSTALL_FAILED_DUPLICATE_PERMISSION perm=com.alipay.mobile.command.trigger.permission pkg=com.eg.android.AlipayGphone]

Failure [INSTALL_FAILED_SHARED_USER_INCOMPATIBLE: Package couldn't be installed in /data/app/com.miui.wmsvc-sU_DbROKHDH9gQVWxXuimA==: Package com.miui.wmsvc has no signatures that match those in shared user android.uid.system; ignoring!]

```sh
$ adb shell df
error: device unauthorized.
This adb server's $ADB_VENDOR_KEYS is not set
Try 'adb kill-server' if that seems wrong.
Otherwise check for a confirmation dialog on your device.
```

## Install

>据前面安装 SuperSU 的过程，也很简单：在 /system/app 中创建一个新目录，权限为 755 ；再把 apk 复制到刚才的目录里，权限为 644 ；然后 chcon 为 u:object_r:system_file:s0 ；最后重启

解除 TCL 电视的 adb/pm install 安装限制：进入 adb shell 后，使用 tclsu 取得 root shell ，然后执行

```sh
# setprop persist.tcl.installapk.enable 1
# setprop persist.tcl.debug.installapk 1
```

给 TCL 电视安装第三方 Launcher ：进入 root shell 后，执行

```sh
# mount -o remount,rw /system
```

挂载 system 分区为可写，新建 /system/app/<应用名称> 目录，权限为 0755 ；再将 apk 文件复制到刚才的目录中，权限为 0644 。

    打开电视，进入直播，选择 TV 信源
    按遥控器上的“设置”按钮，进入显示 -> 高级设定，然后把焦点移动在“对比度”上，但不要进入
    快速依次按下遥控器上的 1 9 5 0 数字键，稍等几秒后便会打开“Main menu/Factory”工厂模式菜单 1 2
    选择 Other -> ADB ，按下遥控器中的 OK 键将其从 OFF 改为 ON

连接 WiFi ADB ，并进入 adb shell 了：

$ adb connect $TV_IP_ADDR:5555
connected to xxx.xxx.xx.x:5555

https://rocka.me/article/cursed-tcl-android-tv

pm uninstall --user

https://blog.fyun.org/tcl-root.html

