

$ pacmd
Command 'pacmd' not found, but can be installed with:
sudo apt install pulseaudio-utils

$ aplay
Command 'aplay' not found, but can be installed with:
sudo apt install alsa-utils

$ alsamixer
Command 'alsamixer' not found, but can be installed with:
sudo apt install alsa-utils

$ pavucontrol
Command 'pavucontrol' not found, but can be installed with:
sudo apt install pavucontrol


