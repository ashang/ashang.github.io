
# pkill -9 -u aaron


import -window root ${FNAME}.${EXT}

import $HOME/pix/screenshot/$f.png


((f=$f+1))


$ for i in * .*; do if diff -q "$i" ../"$i"; then echo "RM  $i"; rm ../"$i"; else if test -f ../"$i"; then mv -n ../"$i" "$(basename $i ${i##*.}).alt.${i##*.}"; fi;fi;done



⌘ Q : Quit
⌘ W : Close Window
⌘ B : Bold text
⌘ I : Italicize text
⌘ U : Underline text
⌘ O : Open
⌘ P : Print
⌘ A : Select All
⌘ S : Save
⌘ F : Find
⌘ G : Find Again (the G key is next to the F key on a QWERTY keyboard)
⌘ Z : Undo (resembles the action of striking out a mistake)
⌘ X : Cut (resembles scissors – and the X key is next to the C key on a QWERTY keyboard)
⌘ C : Copy
⌘ V : Paste (resembles an arrow pointing downward "into" the document, or a brush used for applying paste, as well as the proofreader's mark for "insert" – and the V key is next to the C key on a QWERTY keyboard)
⌘ N : New Document
⌘ . (full stop): User interrupt
⌘ ? : Help (? signifies a question or confusion)[6]
Later environments such as Microsoft Windows retain some of these bindings, while adding their own from alternate standards like Common User Access. The shortcuts on these platforms (or on macOS) are not as strictly standardized across applications as on the early Macintosh user interface, where if a program did not include the function normally carried out by one of the standard keystrokes, guidelines stated that it should not redefine the key to do something else as it would potentially confuse users.[7]







My responsibilities include working in both Python and PHP on complex systems for lead generation and cloud-based call centers.

Software Engineer
Jun 2014 - Present8 years 1 month

Delray Beach, FL

Working in both Python and PHP to advance and optimize our systems to provide redundancy, resilience, and scalability.

FloorForce, LLC. Graphic
PHP Developer
FloorForce, LLC.
Oct 2013 - Mar 20146 months

My responsibilities included creating new projects in conjunction with a separate entity written in .NET that acted as our data storage and feed aggregation application. I further developed applications which used APIs to communicate with the .NET side to provide a friendly user experience to our clients' customers. I also handled the transition of two of our top developers leaving the company in the same week while also striving to maintain development and forward momentum for the company architecture.



