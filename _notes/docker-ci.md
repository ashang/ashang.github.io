
```
volume -v /var/run/docker.sock:/var/run/docker.sock:ro
```

## Kaniko

## drone.io

## gitlab-runner (running on docker) to make the build.

The images are build with Kaniko directly by the server and pushed to gitlab registry.

