## lz4json

: unpack lz4json files, usually generated by Mozilla programs

 Instead of a standard .json.lz4, Firefox uses its own format to compress
 its bookmarks and session restore files.  This tool lets you read them,
 converting to json.  Going from json to a human-readable format is then
 up to you.

Homepage: https://github.com/andikleen/lz4json


jq -r ".windows[0].tabs | sort_by(.lastAccessed)[-1] | .entries[.index-1] | .url"


$ lz4jsoncat promoz/sessionstore-backups/recovery.jsonlz4 | jq -r ".windows[0].tabs | sort_by(.lastAccessed)[-1] | .entries[.index-1] | .url"
https://askubuntu.com/users/718964/bao-haojun


