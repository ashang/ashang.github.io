---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/css" href="{{ "/assets/css/rss.css" | prepend: site.baseurl }}" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.title | xml_escape }}</title>
    <description>{{ site.description | xml_escape }}</description>
    <atom:link href="{{ "/feed.xml" | prepend: site.baseurl | prepend: site.url }}" rel="self" type="application/rss+xml"/>

    {% for post in site.note limit:42 %}
      <item>
        <title>{{ post.title | xml_escape }}</title>
        <description>{% if post.place %} &lt;p&gt; Location: {{ post.place | xml_escape }} &lt;/p&gt; {% endif %}{{ post.content | xml_escape }}</description>
        {% if post.author %}<author>{{ post.author | xml_escape }}</author>{% endif %}
        <pubDate>{{ post.date | date_to_rfc822 }}</pubDate>
        <link>{{ post.url | prepend: site.baseurl | prepend: site.url }}</link>
        <guid isPermaLink="true">{{ post.url | prepend: site.baseurl | prepend: site.url }}</guid>

        {% for tag in post.tags %}
          <category>{{ tag | xml_escape }}</category>
        {% endfor %}
      </item>
    {% endfor %}
  </channel>
</rss>
